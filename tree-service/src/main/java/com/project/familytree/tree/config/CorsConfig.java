package com.project.familytree.tree.config;

// This file is intentionally empty.
// CORS is configured exclusively in the family-tree-auth-starter SecurityConfig.corsConfigurationSource()
// which reads app.base-url from application.properties.
// Having a duplicate CorsFilter bean here would conflict with the starter's SecurityFilterChain CORS config.
